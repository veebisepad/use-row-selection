/**
 * Vue Row Selection - Headless row selection state management for Vue 3 tables
 * @module vue-row-selection
 */

export * from './types';

// Re-export core composable
export { useRowSelection } from './useRowSelection';
